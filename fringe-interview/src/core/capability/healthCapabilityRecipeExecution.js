const cap=require('./index');const dim=require('../dimension');function healthCapabilityRecipeExecution(){try{const now='2026-07-23T12:00:00.000Z';const c=dim.buildDimensionContribution({id:'health-cap-recipe-c',measurementId:'health-cap-recipe-m',dimensionId:'health_dimension',contributionType:'supporting',contributionValue:.8,confidence:.8,provenance:{measurementResultRef:'measurementResult:health',sourceRefs:['mapping:health']},metadata:{version:'1.0',createdAt:now,updatedAt:now},extensions:{}},{now});const l=dim.appendDimensionContributions(dim.buildKnowledgeLedger({metadata:{createdAt:now,updatedAt:now}},{now}),[c],{now});const s=dim.buildKnowledgeSnapshot(l,{now});const rule=dim.buildDerivedKnowledgeRule({target:{knowledgeType:'derived_signal',knowledgeId:'health'},conditions:[{dimensionId:'health_dimension',field:'estimate',operator:'gte',value:.8,minimumConfidence:.5,minimumCoverage:.5,extensions:{}}],conditionStrategy:'all',confidenceStrategy:'minimum',output:{valueType:'boolean',value:true},metadata:{createdAt:now,updatedAt:now},extensions:{}},{now});const recipe=cap.buildCapabilityRecipe({capabilityId:'health_capability',version:'1.0.0',rules:[rule],extensions:{}},{now});const result=cap.executeCapabilityRecipe(s,recipe,{now});return{ok:cap.validateCapabilityExecutionResult(result).valid&&result.derivedResults.length===1}}catch(e){return{ok:false,error:e.message}}}module.exports={healthCapabilityRecipeExecution};
