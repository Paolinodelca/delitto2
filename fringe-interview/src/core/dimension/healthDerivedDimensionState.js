const dim=require('./index');const cap=require('../capability');
async function healthDerivedDimensionState(){try{const now='2026-07-23T12:00:00.000Z';const contribution=dim.buildDimensionContribution({id:'health_dds_c1',measurementId:'health_dds_m1',dimensionId:'source_dimension',contributionType:'supporting',contributionValue:.9,confidence:.8,provenance:{measurementResultRef:'measurementResult:health_dds',sourceRefs:['mapping:health_dds']},metadata:{version:'1.0',createdAt:now,updatedAt:now},extensions:{}},{now});let ledger=dim.buildKnowledgeLedger({metadata:{createdAt:now,updatedAt:now}},{now});ledger=dim.appendDimensionContributions(ledger,[contribution],{now});const snapshot=dim.buildKnowledgeSnapshot(ledger,{now});const rule=dim.buildDerivedKnowledgeRule({target:{knowledgeType:'derived_dimension',knowledgeId:'target_dimension_signal'},conditions:[{dimensionId:'source_dimension',field:'estimate',operator:'gte',value:.8,minimumConfidence:.5,minimumCoverage:.5,extensions:{}}],conditionStrategy:'all',confidenceStrategy:'minimum',output:{valueType:'boolean',value:true},metadata:{createdAt:now,updatedAt:now},extensions:{}},{now});const recipe=cap.buildCapabilityRecipe({capabilityId:'capability:health_dds',version:'1.0.0',rules:[rule],extensions:{}},{now});const execution=cap.executeCapabilityRecipe(snapshot,recipe,{now});const mapping=dim.buildDerivedDimensionMapping({resultTarget:rule.target,dimensionId:'derived_dimension_health',estimate:.85,extensions:{}},{now});const states=dim.buildDerivedDimensionKnowledgeStates([execution],[mapping],{now});if(states.length!==1||!dim.validateDerivedDimensionKnowledgeState(states[0]).valid)throw new Error('Derived state pipeline failed.');return{ok:true}}catch(error){return{ok:false,error:error.message}}}
module.exports={healthDerivedDimensionState};
