const {buildDerivedDimensionKnowledgeState}=require('./buildDerivedDimensionKnowledgeState');const TOP=['id','dimensionId','knowledgeLayer','stateType','status','estimate','confidence','capabilityId','recipeRef','recipeVersion','snapshotRef','executionRefs','sourceResultRefs','dependencyRefs','provenance','derivedAt','metadata','extensions'];function obj(v){return v!==null&&typeof v==='object'&&!Array.isArray(v)}function str(v){return typeof v==='string'&&v.trim().length>0}function unit(v){return typeof v==='number'&&Number.isFinite(v)&&v>=0&&v<=1}function iso(v){return typeof v==='string'&&!Number.isNaN(Date.parse(v))&&new Date(v).toISOString()===v}function refs(v,p,e,prefix){if(!Array.isArray(v)||new Set(v).size!==v.length||[...v].sort().join('|')!==v.join('|')||v.some(x=>!str(x)||(prefix&&!x.startsWith(prefix))))e.push(`${p} is invalid.`)}function raw(v){if(!obj(v)&&!Array.isArray(v))return false;if(obj(v)&&Object.keys(v).some(k=>['rawPayload','payload','knowledgeSnapshot','dimensionContribution','measurementResult','observation'].includes(k)))return true;return Object.values(v).some(raw)}
function validateDerivedDimensionKnowledgeState(s={}){const errors=[],warnings=[];if(!obj(s))return{valid:false,errors:['DerivedDimensionKnowledgeState must be an object.'],warnings};Object.keys(s).filter(k=>!TOP.includes(k)).forEach(k=>errors.push(`derivedDimensionKnowledgeState.${k} is not allowed.`));if(!str(s.id)||!str(s.dimensionId))errors.push('id and dimensionId are required.');if(s.knowledgeLayer!=='derived'||s.stateType!=='derived'||s.status!=='known')errors.push('derived discriminants are invalid.');if(!unit(s.estimate)||!unit(s.confidence))errors.push('estimate and confidence must be between 0 and 1.');if(!str(s.capabilityId))errors.push('capabilityId is required.');if(!str(s.recipeRef)||!s.recipeRef.startsWith('capabilityRecipe:'))errors.push('recipeRef is invalid.');if(!str(s.recipeVersion))errors.push('recipeVersion is required.');if(!str(s.snapshotRef)||!s.snapshotRef.startsWith('knowledgeSnapshot:'))errors.push('snapshotRef is invalid.');refs(s.executionRefs,'executionRefs',errors,'capabilityExecutionResult:');refs(s.sourceResultRefs,'sourceResultRefs',errors,'derivedKnowledgeResult:');refs(s.dependencyRefs,'dependencyRefs',errors);if(!s.executionRefs.length||!s.sourceResultRefs.length)errors.push('source references must be non-empty.');if(!obj(s.provenance)||s.provenance.type!=='derived_dimension_aggregation'||s.provenance.producerVersion!=='1.0')errors.push('provenance is invalid.');if(!iso(s.derivedAt))errors.push('derivedAt is invalid.');if(!obj(s.metadata)||s.metadata.contractVersion!=='1.0'||Object.keys(s.metadata).some(k=>k!=='contractVersion'))errors.push('metadata is invalid.');if(!obj(s.extensions))errors.push('extensions must be an object.');if(raw(s))errors.push('DerivedDimensionKnowledgeState must not contain raw payloads.');if(errors.length===0&&s.id!==buildDerivedDimensionKnowledgeState(s,{now:s.derivedAt}).id)errors.push('id does not match canonical state identity.');return{valid:errors.length===0,errors,warnings}}
module.exports={validateDerivedDimensionKnowledgeState};
