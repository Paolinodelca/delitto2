/**
 * FRINGE Interview Engine
 *
 * Aggiorna continuamente lo stato
 * del colloquio.
 *
 * Decide:
 * - cosa è stato osservato
 * - cosa manca
 * - se serve un follow-up
 * - quale segnale affrontare dopo
 */

export function updateInterviewState({
  interviewState,
  lastAnswer,
  evidencePlan
}) {
  return interviewState;
}