const {TYPES,SCOPES,REASONS}=require('./validateKnowledgeOpportunity');
const FIELDS=['opportunityType','scope','scopeRef','coverageState','knowledgeLayer','reasonCode','sourceCoverageRef'];
const COVERAGE_STATES=['elementary_only','derived_only','available'];
function str(v){return typeof v==='string'&&v.trim().length>0&&v===v.trim()}
function validateKnowledgeOpportunityQuery(q){const errors=[],warnings=[];if(!q||typeof q!=='object'||Array.isArray(q))return{valid:false,errors:['KnowledgeOpportunityQuery must be an object.'],warnings};const keys=Object.keys(q);for(const k of keys)if(!FIELDS.includes(k))errors.push(`Unknown property: ${k}.`);if(!keys.some(k=>FIELDS.includes(k)))errors.push('At least one query filter is required.');for(const k of ['scopeRef','sourceCoverageRef'])if(q[k]!==undefined&&!str(q[k]))errors.push(`${k} must be a non-empty canonical string.`);if(q.opportunityType!==undefined&&!TYPES.includes(q.opportunityType))errors.push('opportunityType is invalid.');if(q.scope!==undefined&&!SCOPES.includes(q.scope))errors.push('scope is invalid.');if(q.coverageState!==undefined&&!COVERAGE_STATES.includes(q.coverageState))errors.push('coverageState is invalid.');if(q.knowledgeLayer!==undefined&&!['elementary','derived'].includes(q.knowledgeLayer))errors.push('knowledgeLayer is invalid.');if(q.reasonCode!==undefined&&!REASONS.includes(q.reasonCode))errors.push('reasonCode is invalid.');return{valid:errors.length===0,errors,warnings}}
module.exports={validateKnowledgeOpportunityQuery,FIELDS};
