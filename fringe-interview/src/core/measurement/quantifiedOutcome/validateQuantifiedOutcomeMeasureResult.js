function obj(v){return v!==null&&typeof v==='object'&&!Array.isArray(v)}
function validateQuantifiedOutcomeMeasureResult(r){const errors=[];if(!obj(r))return{isValid:false,errors:['MeasureResult must be an object.']};if(r.measureId!=='quantified_outcome')errors.push('measureId is invalid.');if(!['draft','insufficient'].includes(r.resultStatus))errors.push('resultStatus is invalid.');if(r.resultStatus==='draft'&&r.presence!==1)errors.push('presence must be 1 for a supported quantified outcome.');if(r.resultStatus==='draft'&&!obj(r.semanticDetail))errors.push('semanticDetail is required.');if(!Array.isArray(r.evidenceIds)||!obj(r.context)||!Array.isArray(r.limitations)||!obj(r.metadata)||!obj(r.extensions))errors.push('MeasureResult structure is invalid.');return{isValid:errors.length===0,errors};}
module.exports={validateQuantifiedOutcomeMeasureResult};
