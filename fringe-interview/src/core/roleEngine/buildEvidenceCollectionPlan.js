/**
 * FRINGE Role Engine
 *
 * Trasforma la Role Credibility Map
 * in una strategia di raccolta evidenze.
 *
 * Non genera ancora il colloquio.
 */

export function buildEvidenceCollectionPlan({
  roleCredibilityMap
}) {
  return {
    signals: [],
    followupPolicies: [],
    extensions: {}
  };
}