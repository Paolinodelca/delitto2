const know=require('../src/core/knowledge');const {buildCoverage}=require('./knowledge_opportunity_fixture');const coverage=buildCoverage();const result=know.healthKnowledgeOpportunity(coverage);if(!result.ok||!know.validateKnowledgeOpportunityCollection(result.result).valid)throw new Error('Knowledge Opportunity health failed.');const empty=know.evaluateKnowledgeOpportunities(coverage,{overallCoverageState:'empty'});if(!empty.summary.empty)throw new Error('Empty opportunity health case failed.');console.log('Knowledge Opportunity health PASSED');
