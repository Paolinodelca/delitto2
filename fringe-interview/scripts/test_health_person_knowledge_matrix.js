const dim=require('../src/core/dimension');const cap=require('../src/core/capability');const {healthPersonKnowledgeMatrix}=require('../src/core/knowledge/healthPersonKnowledgeMatrix');const now='2026-07-23T12:00:00.000Z';const c=dim.buildDimensionContribution({id:'health-pkm-c',measurementId:'health-pkm-m',dimensionId:'health_source',contributionType:'supporting',contributionValue:.9,confidence:.8,provenance:{measurementResultRef:'measurementResult:health',sourceRefs:['mapping:health']},metadata:{version:'1.0',createdAt:now,updatedAt:now},extensions:{}},{now});let l=dim.buildKnowledgeLedger({metadata:{createdAt:now,updatedAt:now}},{now});l=dim.appendDimensionContributions(l,[c],{now});const s=dim.buildKnowledgeSnapshot(l,{now});const r=dim.buildDerivedKnowledgeRule({target:{knowledgeType:'derived_dimension',knowledgeId:'health_target'},conditions:[{dimensionId:'health_source',field:'estimate',operator:'gte',value:.7,minimumConfidence:.5,minimumCoverage:.5,extensions:{}}],conditionStrategy:'all',confidenceStrategy:'minimum',output:{valueType:'boolean',value:true},metadata:{createdAt:now,updatedAt:now},extensions:{}},{now});const recipe=cap.buildCapabilityRecipe({capabilityId:'cap:health-pkm',version:'1.0.0',rules:[r],extensions:{}},{now});const execution=cap.executeCapabilityRecipe(s,recipe,{now});const mapping=dim.buildDerivedDimensionMapping({resultTarget:r.target,dimensionId:'health_derived',estimate:.85,extensions:{}},{now});const derived=dim.buildDerivedDimensionKnowledgeStates([execution],[mapping],{now});const result=healthPersonKnowledgeMatrix(s,derived,now);if(!result.ok||result.matrix.summary.elementaryStateCount<1||result.matrix.summary.derivedStateCount<1)throw new Error('Health failed');console.log('Person Knowledge Matrix health PASSED');
