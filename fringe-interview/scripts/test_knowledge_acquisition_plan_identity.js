const assert=require('assert');const {buildKnowledgeAcquisitionPlan}=require('../src/app/knowledge');const {calculateKnowledgeAcquisitionPlanId}=require('../src/app/knowledge/knowledgeAcquisitionPlanIdentity');const F=require('./knowledge_acquisition_plan_fixture');
const f=F.buildFixture(),a=buildKnowledgeAcquisitionPlan(f.composed.input),b=buildKnowledgeAcquisitionPlan(f.composed.input);assert.equal(a.id,b.id);assert.equal(a.id,calculateKnowledgeAcquisitionPlanId(a));const changed={...a,extensions:{identityVariant:true}};changed.id=calculateKnowledgeAcquisitionPlanId(changed);assert.notEqual(changed.id,a.id);console.log('Knowledge Acquisition Plan identity tests passed.');
