const assert=require('assert');const api=require('../src/app/knowledge');for(const name of ['buildKnowledgeAcquisitionPlan','validateKnowledgeAcquisitionPlan','validateKnowledgeAcquisitionPlanContext','healthKnowledgeAcquisitionPlan'])assert.equal(typeof api[name],'function',name);assert(!('buildKnowledgeAcquisitionPlan'in require('../src/core/knowledge')));console.log('Knowledge Acquisition Plan public API tests passed.');
