const assert=require('assert');const {healthKnowledgeAcquisitionCapabilityConfiguration}=require('../src/app/knowledge');const fixture=require('./knowledge_acquisition_capability_configuration_fixture');const result=healthKnowledgeAcquisitionCapabilityConfiguration(fixture);assert(result.ok,JSON.stringify(result));console.log('Knowledge Acquisition Capability Configuration health PASSED');
