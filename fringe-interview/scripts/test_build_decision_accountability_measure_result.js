const assert=require('assert');const M=require('../src/core/measurement/decisionAccountability');const {projectDecisionAccountabilityMeasureResult}=require('../src/core/measurement/decisionAccountability/projectDecisionAccountabilityMeasureResult');
function obs(o={}){return M.buildDecisionAccountabilityObservation({observationId:'obs:measure',decisionAuthority:'final',consequenceScope:'site',accountabilityEvidence:'explicit_with_outcomes',responsibilityContinuity:{state:'known',qualification:'exact',months:30},context:{decision:'release'},evidenceIds:['ev:1'],inferenceSupportInputs:{evidenceQuality:{state:'known',value:.9,producer:{producerRef:'evidenceAssessment:1'}},sourceConvergence:{state:'known',value:.85,producer:{producerRef:'convergence:1'}},consistency:{state:'known',value:.9,producer:{producerRef:'consistency:1'}},coverage:{state:'known',value:.8,producer:{producerRef:'coverage:1'}}},...o})}
const full=M.buildDecisionAccountabilityMeasureResult({observation:obs()});assert.equal(full.resultStatus,'draft');assert.equal(full.score,.9625);assert.equal(full.inferenceSupport.value,.8675);assert(M.validateDecisionAccountabilityMeasureResult(full).isValid);assert(projectDecisionAccountabilityMeasureResult(full));
const unknownOptional=M.buildDecisionAccountabilityMeasureResult({observation:obs({accountabilityEvidence:undefined})});assert.equal(unknownOptional.resultStatus,'insufficient');assert.equal(unknownOptional.score,null);assert.equal(unknownOptional.components.accountabilityEvidenceScore,null);assert.equal(projectDecisionAccountabilityMeasureResult(unknownOptional),null);
const qualified=M.buildDecisionAccountabilityMeasureResult({observation:obs({responsibilityContinuity:{state:'known',qualification:'approximate',months:12}})});assert.equal(qualified.resultStatus,'insufficient');assert.equal(qualified.components.responsibilityContinuityScore,null);assert.equal(qualified.score,null);
const partialInf=M.buildDecisionAccountabilityMeasureResult({observation:obs({inferenceSupportInputs:{evidenceQuality:{state:'known',value:.9,producer:{producerRef:'evidenceAssessment:1'}},sourceConvergence:{state:'not_yet_derived'},consistency:{state:'unknown'},coverage:{state:'not_yet_derived'}}})});assert.equal(partialInf.resultStatus,'draft');assert.equal(partialInf.score,.9625);assert.equal(partialInf.inferenceSupport.state,'partial');assert.equal(partialInf.inferenceSupport.value,null);assert.equal(projectDecisionAccountabilityMeasureResult(partialInf),null);
const none=M.buildDecisionAccountabilityMeasureResult({observation:obs({decisionAuthority:'none'})});assert.equal(none.resultStatus,'contextual');assert.equal(none.score,null);assert.equal(projectDecisionAccountabilityMeasureResult(none),null);
const missingRequired=M.buildDecisionAccountabilityMeasureResult({observation:obs({consequenceScope:undefined})});assert.equal(missingRequired.resultStatus,'insufficient');assert.equal(projectDecisionAccountabilityMeasureResult(missingRequired),null);
console.log('Decision Accountability Measure Result unknown-safe tests passed.');
