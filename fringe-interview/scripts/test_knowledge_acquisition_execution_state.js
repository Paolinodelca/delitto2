const assert=require('assert');const A=require('../src/app/knowledge');const F=require('./knowledge_acquisition_execution_fixture');const f=F.buildFixture(),context=(x,targetStatus)=>({knowledgeAcquisitionExecution:x,knowledgeAcquisitionRuntimeSession:f.session,knowledgeAcquisitionPlan:f.plan,targetStatus}),created=A.buildKnowledgeAcquisitionExecution(f.input,{now:f.now});assert.throws(()=>A.transitionKnowledgeAcquisitionExecution(context(created,'ready_for_invocation'),{now:f.later}),e=>e.code==='ILLEGAL_EXECUTION_TRANSITION');const selected=A.transitionKnowledgeAcquisitionExecution(context(created,'selected'),{now:f.later});assert.equal(selected.status,'selected');assert.deepEqual(selected.selectedUnit,{sourcePlanItemRef:created.sourcePlanItemRef});assert.equal(selected.readiness.readyForInvocation,false);const ready=A.transitionKnowledgeAcquisitionExecution(context(selected,'ready_for_invocation'),{now:f.latest});assert.equal(ready.status,'ready_for_invocation');assert.equal(ready.readiness.readyForInvocation,true);assert.equal(ready.id,created.id);assert.throws(()=>A.transitionKnowledgeAcquisitionExecution(context(ready,'selected'),{now:f.latest}),e=>e.code==='ILLEGAL_EXECUTION_TRANSITION');assert.equal(created.status,'created');assert.equal(selected.status,'selected');console.log('Knowledge Acquisition Execution state machine tests passed.');
